#include <iostream>
using namespace std;
main()
{
    int a, b;
    cout << "Enter two numbers: " << endl;
    cin >> a >> b;
    if (a == b)
        cout << "The numbers are equal." << endl;
    else
        cout << "The numbers are not equal." << endl;
}